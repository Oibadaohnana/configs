
git:

git init
# . alle Dateien in diesem Ordner
git add . 
#
git add "Dateiname"

git commit -m "commit message"

#schick git auf den remote "repository" auf gitHub z.B.
git push origin main
#repository in neuen Pc schreiben
#fuer neues aufsetzten dann auch noch harware config Device name anpassen
git clone git@github.com:Oibadaohnana/configs
fdisk -l



#What I have done and want to document:
[benji@Bdawg:~]$ systemctl --user mask plasma-workspace@.service
Unit plasma-workspace@.service does not exist, proceeding anyway.
Created symlink '/home/benji/.config/systemd/user/plasma-workspace@.service' → '/dev/null'.

[benji@Bdawg:~]$ systemctl --user mask ksmserver-logout-greeter.service
Unit ksmserver-logout-greeter.service does not exist, proceeding anyway.
Created symlink '/home/benji/.config/systemd/user/ksmserver-logout-greeter.service' → '/dev/null'.


#Update Flakes:
nix flakes update
ls -la /home/benji/nixcfg/.git/objects 
#delete old generations
sudo nix-collect-garbage -d
sudo nixos-rebuild boot --flake nixcfg/nixos#benji
sudo nixos-rebuild boot --flake ~/nixos#benji
#wenn irgendein .git nicht von User controlled dann:
sudo chown -R benji:users /home/benji/nixcfg/.git

#csharp
dotnet new console -n <Project NAME>
dotnet build
dotnet run
